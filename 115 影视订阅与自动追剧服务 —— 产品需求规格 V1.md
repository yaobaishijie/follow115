# 115 影视订阅与自动追剧服务 —— 产品需求规格 V1

> 文档状态：V1 最终交付稿  
> 产品定位：专门面向 115 网盘的影视订阅、自动追更、缺集补齐与资源整理服务  
> 技术路线：**新产品作为独立自研服务实现，不运行、不依赖 CloudSaver 服务。CloudSaver 0.9.0 与历史 subscriber 仅作为逆向参考，按需复刻其已验证逻辑。**  
> 核心原则：**用户只决定“我要不要追”，其他事情交给系统。**

---

# 1. 产品定位与设计原则

## 1.1 一句话定位

**一个面向 115 网盘用户的影视订阅与自动追剧服务：用户找到想看的影视后点击“追剧”，系统自动搜索资源、检查更新、补齐缺集、转存/离线下载、整理目录，并持续维护 115 中的影视内容。**

## 1.2 核心对象

CloudSaver 原本偏“资源搜索工具”，新产品的核心对象改为：

```text
影视订阅
```

核心链路：

```text
影视
 ↓
追剧
 ↓
资源发现
 ↓
115 目录扫描
 ↓
缺集判断
 ↓
自动补齐
 ↓
文件验证与整理
 ↓
持续追更
```

## 1.3 产品原则

1. **不把用户当资源管理员。** 用户不需要理解资源群、磁力、InfoHash、候选链路等技术细节。
2. **前台看结果，后台做执行。** 前台展示“更新到多少、115 有多少、缺哪些、当前状态”。
3. **独立自研服务。** 仅复用/重写 CloudSaver 中已验证且必要的热门影视、Search Channels、115 等逻辑，不保留 CloudSaver 运行时依赖。
4. **只服务 115。** V1 不做多网盘兼容。
5. **极简交互。** 追剧、恢复、重新追剧等操作尽量一点即生效。
6. **不阻塞页面。** 115 扫描、资源搜索、离线验证均异步执行。
7. **PC 与 Mobile 分别设计。** 功能一致，但不做简单缩放。
8. **整体 UI 风格参考 btbtla 的影视内容站风格。** 海报优先、留白充足、信息层级清晰、按钮克制；不做传统后台管理系统风格。

---

# 2. 信息架构与导航

V1 仅保留 3 个一级入口：

```text
发现 / 追剧 / 设置
```

## 2.1 PC Web

使用顶部导航：

```text
Logo    发现    追剧    设置                         搜索/用户入口
```

## 2.2 Mobile Web

使用底部 Tab：

```text
发现        追剧        设置
⌂           ▣           ⚙
```

移动端顶部仅保留页面标题、Logo 或搜索等必要信息，不重复放一级导航。

---

# 3. 发现页与统一搜索

## 3.1 页面目标

发现页只做两件事：

1. 找当前热门影视；
2. 搜索想看的影视。

不增加轮播图、榜单、公告、资源动态等无关模块。

## 3.2 页面结构

```text
顶部导航

大搜索框
搜索电影、电视剧、综艺、动漫……

热门内容分区
[保持 CloudSaver 0.9.0 原生 11 个热榜分类]

每个分区：分区标题 + 3 列海报网格，每个分类首页展示 3 行（共 9 个海报） + 可选“更多”
```

说明：

- 发现页不再采用顶部分类 Tab 切换作为主要浏览方式；
- 首页改为**同页纵向分区流**：用户向下滚动即可连续浏览不同热门分类；
- 当前设计稿中的分区名称仅作为**占位符**；
- 正式实现时发现页分类保持 CloudSaver 0.9.0 的 11 个热榜分类：`热门电影 / 最新电影 / 冷门佳片 / 热门电视剧 / 热门国产剧 / 热门欧美剧 / 热门韩剧 / 热门日剧 / 热门动画 / 热门纪录片 / 热门综艺`；
- 每个分区首页默认展示 **3 行 × 3 列 = 9 个海报**；
- 分区内影视按**年份倒序**排列；年份从 `card_subtitle` 开头提取，同年份内容保持原接口顺序，不额外补真实上映日期；
- 每个分区可在标题右侧提供轻量 `更多 >`，进入该分类完整列表；
- 这种设计优先保证首屏响应感和连续浏览体验，避免用户每切换一次分类都等待一次页面请求；
- 这里与 115 网盘中的存储分类是两套概念，不能混用：发现分类用于浏览，115 存储分类用于归档。

## 3.3 热门数据来源

热门影视沿用 CloudSaver 0.9.0 已逆向确认的豆瓣热门数据链路，由新服务自行实现，不调用 CloudSaver 服务。

`GET /api/douban/hot` 归一化后的核心字段包括：

- `id`；
- `title`；
- `pic.large / pic.normal`；
- `rating.value / rating.count`；
- `card_subtitle`；
- `episodes_info`；
- `type`；
- `is_new`。

海报角标规则保持 CloudSaver 0.9.0 原生前端统计逻辑：

- 左上角：`rating.value`，格式化为一位小数；无评分显示 `--`；
- 右上角：根据 `rating.value` 计算五档推荐标签：
  - 无评分或 `rating.value <= 0` → `待评分`；
  - `rating.value >= 8.5` → `神作`；
  - `rating.value >= 7.5` → `推荐`；
  - `rating.value >= 6.5` → `可看`；
  - 其他 → `一般`。

V1 不自研新的评分/推荐算法，也不额外引入真实上映日期元数据。

## 3.4 海报卡片

海报卡只保留必要信息：

- 海报；
- 标题；
- 年份 · 类型；
- `更新至 X / Y 集`；
- 左上角 CloudSaver 评分；
- 右上角 CloudSaver 0.9.0 原生五档推荐标签（`待评分 / 神作 / 推荐 / 可看 / 一般`）；
- 右下角轻量订阅状态标识。

示意：

```text
┌──────────────┐
│ 9.2      推荐 │
│              │
│     海报      │
│              │
│       ✓ 已追  │
└──────────────┘
藏海传
2026 · 国产剧
更新至 30 / 40 集
```

首页卡片**不放追剧按钮**。点击整张卡片进入影视详情页。

## 3.5 卡片订阅状态

根据当前状态显示轻量标签：

- `✓ 已追`
- `已暂停`
- `已停追`
- `已保存`（电影）

状态只用于展示，不在卡片上直接放“继续追剧 / 重新追剧”等操作按钮。

## 3.6 统一搜索

搜索框同时负责当前热门与历史影视搜索。

搜索顺序：

```text
本地缓存 / 当前热门影视元数据
        ↓
未找到
        ↓
btbtla 历史影视搜索
```

btbtla 搜索结果直接使用其影视元数据，不从原始资源链接反推元数据。

搜索结果统一转成同一种影视卡片，不展示原始资源链接、磁力或来源列表。

## 3.7 搜索无结果

热门元数据/缓存与 btbtla 都未找到时，只提示：

> 没找到这部影视

不向用户展示技术来源、错误链路或“哪个源失败”。

## 3.8 元数据缓存

建议采用轻量缓存：

- 豆瓣热门：短期缓存；
- btbtla 历史搜索：按需缓存；
- 已订阅影视：长期保留；
- 图片只保存 URL，不保存图片二进制。

建议统一内部 `MediaMetadata`：

```text
id / sourceId
source
name / aliases
year
mediaType
region
genres
posterUrl
backdropUrl
rating
recommendation
latestEpisode
totalEpisodes
summary
```

---

# 4. 影视详情页

## 4.1 详情内容

详情页以影视内容为主，不展示资源列表。

主要内容：

- 背景图 / Hero；
- 海报；
- 标题；
- 年份；
- 类型 / 地区 / 标签；
- 剧情简介；
- 主演 / 导演等元数据；
- `更新至 X 集 / 全 Y 集`；
- 当前追剧状态；
- 已追剧时增加 115 进度摘要。

## 4.2 主按钮状态

根据订阅状态变化：

| 当前状态 | 主按钮 |
|---|---|
| 未订阅 | 追剧 |
| 已暂停 | 继续追剧 |
| 已停追 | 重新追剧 |
| 正在追更 | 查看追剧详情 |
| 电影已保存 | 已保存 / 查看详情 |

## 4.3 操作原则

`追剧 / 继续追剧 / 重新追剧` 均不弹复杂确认框。

点击“追剧”后只提示：

> 已加入追剧

后续扫描 115、搜索资源、补集等操作全部由后台异步执行。

`继续追剧`、`重新追剧` 同理直接生效，并给轻量 Toast。

## 4.4 已订阅信息

已订阅时可显示：

```text
最新资源：30 集
115 已有：29 / 30
缺失：30
状态：等待更新
```

只显示最终综合结果，不显示：

```text
热门/资源元数据最新：30
btbtla 最新：29
```

---

# 5. 追剧页

## 5.1 顶部结构

```text
我的追剧

追更中 8    异常 2    待清理 4

当前追剧 | 历史
```

说明：

- `异常`、`待清理` 是小型入口，不做一级菜单；
- 点击数字进入对应轻量页面。

## 5.2 当前追剧排序

默认按**订阅创建时间倒序**：

> 最新订阅的影视排在最前。

异常、补集中等运行状态不主动改变卡片位置，避免列表频繁跳动。

V1 不强制增加复杂排序器。

## 5.3 PC 卡片

PC 使用横向信息卡。**卡片本身必须直接展示核心状态，不要求用户进入技术详情页才能知道当前追更情况。**

建议固定展示：

- 海报；
- 标题；
- 年份 · 类型；
- **当前季：Season XX**；
- **最新集：E XX**；
- **115 已有：X / Y**；
- **缺失集：E03、E07… / 无缺失**；
- **目标画质：4K / 2160P 或 1080P**；
- **115 存储路径**；
- 运行状态；
- 最近检查时间。

卡片直接提供以下操作：

- `升级画质`；
- `暂停 / 恢复`；
- `停追`；
- `释放空间`。

其中：

- `升级画质` 是**订阅级手动触发**，不做全局自动批量升级；
- 仍有 `missingEpisodes` 时，优先补集，暂不执行画质升级；
- `释放空间` 为危险操作，点击后仍需轻量确认。

## 5.4 Mobile 卡片

移动端使用**紧凑横向卡片**，但不能为了压缩高度而隐藏关键状态。

建议布局：

```text
┌────────────────────────────────────┐
│ 海报  标题 · Season 02        追更中 │
│       最新 E08 · 已有 7/8            │
│       缺失 E08 · 目标 4K/2160P      │
│       115：影视库/美剧/剧名/Season02 │
│                         升级  暂停   │
│                         停追  释放   │
└────────────────────────────────────┘
```

原则：

- 左侧固定竖版海报；
- 中间优先展示当前季、最新集、已有集、缺失集、目标画质、115 存储路径；
- 右侧或卡片底部紧凑操作区提供 `升级画质 / 暂停 / 停追 / 释放空间`；
- 不展示剧集状态网格；
- 不展示磁力、InfoHash、资源频道等技术细节；
- 操作区保持紧凑，不额外拆成多个大按钮区。

## 5.5 卡片状态

### 订阅状态

订阅状态只定义：

- `追更中`
- `已暂停`
- `停追`

“已完结”是影视元数据，不是订阅状态。

### 运行状态

独立于订阅状态：

- `等待更新`
- `检查中`
- `补集中`
- `异常`
- `已释放`

## 5.6 当前追剧与历史的边界

当前追剧中不显示：

- 已停追的剧；
- 已保存完成的电影。

上述内容进入历史。

---

# 6. 历史页

## 6.1 筛选

```text
全部 | 已完成剧集 | 已完成电影 | 已停追
```

电视剧 Season 在满足“完结确认 + `missingEpisodes=[]`”后进入 `completed` 生命周期状态，停止小时级调度，并从“当前追剧”移动到历史中的“已完成剧集”。

## 6.2 排序

按**最近变更时间倒序**。

## 6.3 卡片内容

- 海报；
- 名称；
- 年份 · 类型；
- 最终状态；
- 115 是否仍有文件；
- 最近变更时间；
- 对应恢复动作。

动作：

- 已完成剧集：`重新追 / 重新检查`；
- 已停追剧集：`重新追`；
- 已完成电影：`重新获取`。

如果 115 中内容已清空，可显示 `已释放`；仍保留则显示 `115 已保留`。

### Mobile 历史卡片

历史页移动端沿用当前追剧的紧凑横向卡片结构：

- 海报在左；
- 名称、年份 / 类型、最终状态、115 保留状态、最近变更时间放在中部；
- `重新追` / `重新获取` 等动作放在右侧；
- 所有信息保持在同一卡片高度内，不让单独字段或动作额外占用下一整行。

---

# 7. 异常页与待清理页

## 7.1 异常判定

不是一次失败就标异常。

规则：

> **候选级失败不直接进入异常；单轮最多 2 个有效候选都无法补齐，只记录本轮失败；连续 2 轮调度仍未补齐当前待补集，才进入“异常”。**

示例：

```text
10:00 第 1 次失败 → 仍显示等待更新
11:00 第 2 次失败 → 标记异常
12:00 成功补齐     → 自动恢复正常
```

异常不是锁死状态，后续调度成功后自动退出异常，无需用户手工关闭。

## 7.2 异常页

只显示真正需要关注的结果：

```text
海报  剧名
缺失：17、30
已连续 2 轮未补齐
最后检查：18 分钟前
原因：暂无可用资源

[立即检查] [查看详情]
```

不提供换源、手工磁力、手工频道等技术操作。

### Mobile 异常卡片

移动端异常页同样采用紧凑横向卡片：

- 海报在左；
- 标题、缺失集、连续失败轮次、原因、最后检查时间集中在中部；
- `立即检查` / `查看详情` 放在右侧或右下角操作区域；
- 单个原因、时间或动作不能拆成卡片下方的独立整行；
- 卡片高度保持紧凑，和当前追剧 / 历史的移动端视觉结构一致。

异常恢复后：

- 自动从异常页消失；
- 顶部异常计数自动减少；
- 不主动发通知；
- 最近活动留下 `已恢复，缺失剧集已补齐`。

## 7.3 待清理定义

待清理只处理**确定的同集重复文件**。

不把模糊识别、文件名不确定等问题塞入待清理，避免误删。

## 7.4 待清理规则

同一集出现多个文件时：

### 画质不同

> 保留高画质，清理低画质。

### 画质相同

> 保留添加时间更早的文件，清理添加时间最新的文件。

原因：旧文件更可能已被影视软件扫描并建立索引，优先保留可减少媒体库索引变化。

## 7.5 待清理页

只显示异常点和清理动作：

```text
共 3 项待清理                        [全部清理]

藏海传
第 18 集存在重复文件
保留：2160P · 较早加入
清理：1080P · 后加入
                                    [一键清理]
```

用户不需要再手工选择哪个文件删，系统提前算好推荐清理对象。

“全部清理”使用轻量确认：

> 将清理 3 个重复文件，保留系统推荐版本。

---

# 8. 115 网盘与释放逻辑

## 8.1 115 是唯一目标端

V1 不兼容其他网盘。

新服务自行实现 115 Adapter，参考 CloudSaver 0.9.0 与历史 subscriber 中已验证的 115 行为，包括：

- 登录/授权；
- 目录查找；
- 目录创建；
- 文件扫描；
- 转存；
- 磁力离线；
- 移动；
- 重命名；
- 目录归类；
- 文件名归一化。

## 8.2 影视类型目录映射

115 一级影视目录统一使用：

```text
影视库
```

其下配置 8 类存储目录：

- 国产剧；
- 美剧；
- 日韩剧；
- 电视剧；
- 综艺；
- 动漫；
- 纪录片；
- 电影。

实际执行必须使用对应 **115 CID**。发现页的 11 个热榜分类与这 8 个存储分类相互独立。

订阅时由系统自动分类，不要求用户选择目录。分类优先级建议：

1. 明确是电影 → `电影`；
2. 明确是纪录片 → `纪录片`；
3. 明确是动画 → `动漫`；
4. 明确是综艺 / 真人秀 → `综艺`；
5. TV + 中国大陆 → `国产剧`；
6. TV + 美国 → `美剧`；
7. TV + 日本 / 韩国 → `日韩剧`；
8. 其他或无法可靠判断的 TV → `电视剧`。

热榜订阅可优先利用热榜来源分类作为强信号；历史影视、统一搜索结果则根据 `type / region / genres / title / year` 等元数据自动判断。首次订阅判断后固化 `storageCategory / targetParentCid / targetFolderCid`，后续调度不重复改变分类，除非用户主动重新绑定目录。

详情页/订阅详情页展示实际存储位置，例如：

```text
115 存储：影视库 / 国产剧 / 藏海传 (2025)
```

## 8.3 文件夹选择器

不要求用户手填 CID。

点击“选择文件夹”后打开 115 目录树：

```text
115
└── 影视库
    ├── 国产剧
    ├── 美剧
    ├── 日韩剧
    ├── 电视剧
    ├── 综艺
    ├── 动漫
    ├── 纪录片
    └── 电影
```

选择后保存：

```text
category
folderName
folderPath   // 用于前端展示
folderCid    // 真正执行使用
```

## 8.4 影视目录创建与季级订阅

电视剧采用**一季一个订阅**，同一剧集共享一个 Series 父目录，每个订阅只绑定一个 `Season XX` 目录。

内部关系：

```text
Series
├── seriesTitle / seriesYear / tmdbId(可选)
├── storageCategory
└── targetSeriesCid

Subscription
├── seriesId
├── seasonNumber / seasonYear
├── targetSeasonCid
└── 本季追更状态
```

创建订阅时：

```text
根据影视元数据自动分类
  ↓
定位对应分类 CID
  ↓
查找 Series 父目录
  ├─ 已存在 → 查找对应 Season XX
  │              ├─ 已存在 → 直接绑定 CID
  │              └─ 不存在 → 创建 Season XX
  └─ 不存在 → 创建 Series 父目录 → 创建 Season XX
```

Series 父目录匹配优先级：

1. `tmdbId` 相同；
2. `seriesTitle + seriesYear` 相同；
3. 标题相同但年份缺失时，仅允许弱匹配，不能强行误绑定。

Season 元数据中的播出年份不能覆盖 Series 首播年份。例如同一剧集第一季首播于 2023、第二季播出于 2025，父目录仍使用 `剧名 (2023)`，第二季订阅单独保存 `seasonYear=2025`。来源标题如“剧名 第二季”需要先归一化为 `seriesTitle=剧名 + seasonNumber=2`，避免为每一季重复创建 Series 父目录。

已有目录**不自动重命名**。例如已有：

```text
最后生还者 (2023)/
└── 第二季/
```

系统应能识别 `第二季 / 第2季 / Season 2 / Season 02 / S02` 均代表 `seasonNumber = 2`，并直接绑定其 CID。只有新建季目录时统一使用：

```text
Season 02
```

### 8.4.1 TMDB 兼容命名规范

为了兼容基于 TMDB 元数据扫描的影视软件，新建目录/文件优先使用以下规范。

电视剧：

```text
剧名 (年份) [tmdbid-ID]/
└── Season 02/
    ├── 剧名 (年份) - S02E01 [2160p].mkv
    └── 剧名 (年份) - S02E02 [1080p].mkv
```

多集文件可使用：

```text
剧名 (年份) - S01E01-E02 [2160p].mkv
```

电影：

```text
片名 (年份) [tmdbid-ID]/
└── 片名 (年份) [2160p].mkv
```

TMDB ID 未获取时，不阻塞目录创建，直接使用：

```text
剧名 (年份)/
片名 (年份)/
```

由于 TMDB 是异步增强，目录创建时只有**本地已经存在可靠 tmdbId** 才追加 `[tmdbid-ID]`；如果目录已经以 `名称 (年份)` 创建，后续异步获得 tmdbId 只写入数据库，不自动重命名已有目录，避免媒体库路径抖动。

### 8.4.2 Season + Episode 唯一标识

电视剧内部不能只保存 `episode=1`，统一使用 `season + episode` 作为唯一剧集身份：

```text
S01E01
S02E01
S00E01
```

解析顺序：

1. 优先从文件名解析 `season + episode`；
2. 文件名没有 season 时，从父目录路径解析 `Season 02 / S02 / 第二季`；
3. 都没有 season 时默认 `season = 1`；
4. 电视剧必须能识别 episode，才能作为有效正片候选。

递归展开 115 分享目录时必须保留 `parentPath` 上下文，禁止像历史 subscriber 一样把多季目录完全拍平后只比较 episode 数字。

### 8.4.3 TMDB 元数据增强（实验性、非阻塞）

TMDB 仅作为**可选、异步的影视身份增强能力**，不是自动追剧主链路依赖。

用户已申请 TMDB API 凭据，配置只在系统配置/环境变量中保存，PRD 不记录真实密钥：

```text
TMDB_ENABLED
TMDB_API_KEY
TMDB_READ_ACCESS_TOKEN
```

策略：

```text
订阅先正常创建
  ↓
目录与资源任务照常执行
  ↓
后台异步按 title + year + mediaType + region 尝试匹配 TMDB
  ↓
成功 → 固化 tmdbId
失败/超时 → tmdbId=null，不影响任何主流程
```

访问策略：

- 默认尝试直连 TMDB；
- 国内网络访问慢/失败时，可复用全局 Clash 代理；
- 查询失败、超时、代理异常不得阻塞订阅创建、目录创建、资源搜索、补集或追更；
- 该能力只用于测试和元数据匹配增强；如果实际收益不明显或影响稳定性，可直接关闭甚至舍弃。

## 8.5 释放

“释放”不是“待清理”。

定义：

> **清空当前订阅对应的 `Season XX` 目录中的全部内容，但保留 Season 文件夹本身、CID、Series 父目录与订阅记录。**

释放后默认：

> **暂停追更。**

防止下一轮调度检测到当前 Season 目录为空后立即把整季重新下载。

确认框只做轻量说明：

- 将删除当前 Season 文件夹中的全部内容；
- 文件夹会保留；
- 订阅记录会保留；
- 释放后暂停追更。

若当前正在补集，先暂停任务再清理。

目录为空时不再显示释放按钮，展示 `已释放`。

V1 仅支持**整季释放**，不做单集释放，也不会影响同一 Series 下的其他 Season。

---

# 9. 每小时调度与资源搜索策略

## 9.1 调度周期

处于 `追更中` 的季订阅默认每 1 小时调度一次。

该周期作为系统内置规则，不在普通设置页暴露。

## 9.2 每轮主流程

```text
每小时触发当前 Season 订阅
   ↓
扫描对应 Season XX 目录
   ↓
得到 existingEpisodeKeys（例如 S02E01、S02E02）
   ↓
查询 Telegram Search Channels
   ↓
必要时查询 btbtla；每 12 小时强制做一次 btbtla 进度校准
   ↓
汇总各来源 latestEpisode
   ↓
计算 resolvedLatestEpisode
   ↓
计算 missingEpisodes
   ↓
生成 115 候选池并过滤 / 排序
   ↓
最多实际尝试 2 个有效候选
   ↓
每个候选执行后重新扫描 115
   ↓
重新计算 missingEpisodes
   ├─ 已补齐 → 结束本轮
   └─ 未补齐 → 尝试第 2 个候选 / 进入失败记录
   ↓
清理非正片文件、整理命名
   ↓
更新订阅状态
```

`missingEpisodes` 同时包含：

- 历史中间缺集；
- 最新更新但 115 尚未存在的集数。

## 9.3 Search Channels 核心资源搜索

新产品不运行 CloudSaver 服务。资源搜索逻辑由新服务直接复刻 CloudSaver 0.9.0 已验证的 Telegram Search Channels 机制：

```text
https://t.me/s/<channel>?q=<keyword>
```

通过 HTTP 抓取 Telegram 公开预览页 HTML，并解析消息内容。**不依赖 Telegram Bot Token，不保留 TG Bot 服务。**

搜索执行策略：

```text
按频道 sort_order 从前到后
→ 固定并发 6 个频道
→ 单频道超时 10 秒
→ 只提取 115 分享链接
→ 找到足够候选后提前停止候选搜索
```

不向用户暴露 CloudSaver 原来的 `wait_all / progressive / useAllChannelSearch / BatchSize` 等策略设置。

“提前停止”只代表当前补集候选已经足够，不代表扫描完所有 Telegram 频道。因此 Telegram 的 `latestEpisode` 只取**本轮实际查询频道**中的最大值；系统继续通过 btbtla 按需查询与每 12 小时校准来避免长期漏掉更高集数。

## 9.4 Search Channels 排序、检测与代理

频道配置至少包含：

```text
id
name
channel_id
is_enabled
sort_order
last_check_status
last_checked_at
last_check_message
```

排序规则：

- 使用显式 `sort_order`；
- 默认初始化时，`name / channel_id` 中包含 `115` 的频道优先；
- 用户拖拽保存后，以用户 `sort_order` 为准，不再自动重排；
- UI 只呈现拖拽顺序，不暴露数值。

频道检测：

- 单个检测；
- 检测全部；
- 新增频道保存前自动检测；
- 检测失败只标记异常，不自动删除、不自动禁用；
- 单频道搜索失败只跳过该频道，不中断全局任务。

代理：

- 设置页沿用 CloudSaver 原代理配置交互；
- 后端沿用 `isProxyEnabled / httpProxyHost / httpProxyPort` 语义；
- Clash 独立 Docker 部署，可复用现有 Docker 配置；
- Telegram、btbtla、TMDB 等外部访问可按配置走 Clash；
- 代理异常不得阻塞主服务启动。

V1 资源解析只保留 115：

- 识别 `115.com / anxia.com / 115cdn.com`；
- 解析 `shareCode / receiveCode`；
- 夸克、123、天翼、百度、阿里、UC、迅雷等链接全部忽略。

## 9.5 latestEpisode 规则

最新集数由多个来源共同推进：

```text
resolvedLatestEpisode = MAX(
  lastResolvedLatestEpisode,
  Telegram/Search Channels latestEpisode,
  btbtla/其他搜索源 latestEpisode
)
```

规则：

- 只允许前进，不允许倒退；
- 搜索源发现比 Telegram 更高的最新集时，也必须推进追更上界；
- btbtla **按需调用 + 每 12 小时兜底校准一次**。

按需触发 btbtla 的典型情况：

- Telegram 没找到有效 115 候选；
- Telegram 候选无法覆盖全部待补集；
- Telegram 无法可靠确认最新集；
- 当前季长时间没有更新，需要额外确认；
- 已明确存在缺集，但 Telegram 无法补全。

### 9.5.1 最新集异常跳增保护

正常增长直接接受；若单轮相对当前 `resolvedLatestEpisode` **跳增超过 10 集**，先不立即推进：

```text
pendingLatestEpisode = 异常高值
```

满足任一条件后才确认：

1. 另一个独立来源也确认到相同/接近集数；
2. 下一轮同一高值再次稳定出现。

集数解析必须来自明确剧集语义，例如：

```text
E19 / EP19 / S01E19 / 第19集 / 19集
```

禁止把 `1080P / 2160P / 2026 / 5000kbps` 等普通数字误识别为集数。

## 9.6 缺集范围

当前季的目标集数集合：

```text
targetEpisodes = 1 ... resolvedLatestEpisode
missingEpisodes = targetEpisodes - existingEpisodes
```

因此既补中间断集，也补最新更新集。

例如：

```text
已有：1,2,3,5,6,7
最新：10
missingEpisodes = [4,8,9,10]
```

## 9.7 资源匹配与 Season 识别

候选基本过滤：

- 标题 / 别名匹配；
- 年份一致优先；
- 当前 `seasonNumber` 必须匹配；
- 电视剧必须能解析出本季 episode；
- 只保留 115 分享；
- 明确低于 1080P 的资源淘汰。

画质归一：

```text
4K / 2160P / UHD / 3840x2160 → 2160p 档
1080P / FHD / 1920x1080        → 1080p 档
无法识别                         → unknown 档
720P 及以下                     → 淘汰
```

最终优先级：

```text
4K / 2160P > 1080P > unknown
```

`4K` 与 `2160P` 视为同一画质档。

## 9.8 候选排序与最多 2 个有效候选

不设计复杂加权评分公式，使用明确 comparator。

排序优先级：

1. **`coversAllMissing=true` 的候选优先**；
2. **`missingCoverageCount` 越大越优先**；
3. **整季包 / 全集包优先**；
4. **画质：4K/2160P > 1080P > unknown**；
5. **`search_channel.sort_order`**；
6. **`preferredGroupKey` 命中**，仅作为最后一级弱偏好。

核心原则：

> **补完整优先于画质。**

例如 A 为 1080P 且可补齐 `E04/E07/E08`，B 为 4K 但只能补 `E07/E08`，优先选择 A。

搜索结果可以很多，但每轮**跨所有资源来源合计最多实际尝试 2 个有效资源方案**。Telegram 115 直连与 btbtla magnet fallback 共用这 2 次执行预算，不是每个来源各自 2 次：

```text
候选 1
→ 转存 / 验证
→ 重新扫描 Season 目录
→ 重算 missingEpisodes
→ 已补齐：结束
→ 仍有缺失：尝试候选 2
```

历史 subscriber 的 `sourceNamingFingerprint` 不迁移；`preferredGroupKey` 仅保留为弱偏好，不改变搜索范围。

## 9.9 115 分享展开、转存与验证

候选真正参与决策前，应调用 115 share-info 并展开真实文件列表；递归展开时保留父目录路径，用于 Season 判断。

电视剧只选择能够识别成当前 Season 正片的有效视频文件进行转存，不把整个分享目录无差别保存。

候选执行后：

1. 重新扫描目标 `Season XX` 目录；
2. 确认目标 `season + episode` 已真实出现；
3. 更新 `existingEpisodes / missingEpisodes`；
4. 再决定是否执行第二个候选。

验证等待时间作为内部实现参数，不在普通 UI 暴露；不得把某次 API 返回成功直接等价为“补集完成”。

资源执行链路明确区分：

```text
Telegram Search Channels
→ 只接受 115 分享链接
→ 115 save / 精确文件转存

btbtla fallback
→ 解析 magnet
→ 提交 115 离线任务
→ 等待落盘验证
```

因此“V1 只保留 115”表示**唯一目标存储端是 115、Telegram 只解析 115 分享**，并不意味着删除 btbtla 的 magnet→115 离线补集链路。

## 9.10 资源失败与永久黑名单

统一维护资源级失败记录：

```text
sourceType          // pan115 / magnet
candidateKey        // 115=shareCode，btbtla magnet=infoHash
failureCount
lastFailedAt
failureReason
isBlacklisted
```

规则：

```text
第一次“资源级失败”
→ 本轮不再重复
→ 下一轮允许再尝试一次

连续第二次“资源级失败”
→ isBlacklisted = true
→ 永久跳过
```

进入永久黑名单后不自动恢复；可预留手工“移出黑名单”能力。btbtla magnet 同样按 `infoHash` 执行两次资源级失败后永久黑名单。

只有能够明确归因于资源本身的问题才累计 `failureCount`，例如：115 分享已失效、shareCode 无效、提取码错误、分享内容不存在、磁力本身无效或离线任务明确返回资源不可用。

以下系统/网络级问题**不得**增加永久黑名单计数：HTTP timeout、DNS 异常、115 服务异常、代理故障、本机网络异常、任务取消等。此类问题只记运行错误，避免把有效资源误拉黑。

## 9.11 正片目录清洁规则

115 最终媒体目录只保留**剧集/电影正片视频文件**。

允许保留的正片扩展名至少：

```text
mkv / mp4 / ts / mov / m2ts
```

以下内容在整理阶段清理，不参与成功判断：

- 字幕文件；
- NFO；
- 图片 / 海报 / 截图；
- 说明文件；
- 压缩包；
- 样片；
- 花絮；
- 预告；
- 其他非正片文件。

这样保证 Season/电影目录干净，降低媒体软件扫描异常风险。

## 9.12 同集重复文件

同集出现多个正片文件时：

- 画质不同：保留高画质；
- 画质相同：保留更早加入的文件；
- 无法可靠判断时不自动误删，进入待清理。

# 10. 电影处理逻辑

电影与电视剧完全分开处理，不要求解析 Episode。

流程：

```text
用户追电影
  ↓
识别为 movie
  ↓
定位“影视库 / 电影”目录
  ↓
搜索 115 候选
  ↓
转存目标电影目录
  ↓
扫描目标文件夹
  ↓
存在有效正片视频文件？
  ├─ 是 → 保存成功 → 标记“已保存” → 停止调度 → 进入历史
  └─ 否 → 当前候选失败 / 尝试下一候选
```

电影保存成功判定只看有效正片视频文件，至少识别：

```text
mkv / mp4 / ts / mov / m2ts
```

分享内存在多个视频时，先排除明显非正片文件（标题/文件名包含 `预告 / trailer / 花絮 / behind / sample / 片段` 等），再从剩余视频中优先选择**体积最大的主视频**作为正片。字幕、NFO、图片、说明文件、压缩包、样片、花絮、预告等不能单独触发“保存成功”，并在整理阶段清理。

电影候选优先级简化为：

```text
4K / 2160P > 1080P > unknown
→ search_channel.sort_order
→ preferredGroupKey（弱偏好）
```

电影保存成功后永久停止周期调度，历史中保留 `重新获取`。

# 11. 订阅详情页

详情页包含 4 个主要区域：

1. 影视基本信息；
2. 追剧进度；
3. 115 存储信息；
4. 最近活动。

## 11.1 PC

建议采用高信息密度左右布局。

追剧进度可显示剧集状态网格：

- 已有；
- 缺失；
- 处理中；
- 异常。

网格主要用于快速看出中间缺集。

## 11.2 Mobile

移动端不显示剧集状态网格。

只显示：

- 最新资源；
- 115 已有；
- 缺失；
- 当前状态；
- 最近检查；
- 释放。

## 11.3 115 信息

可显示：

- 分类；
- Series 父目录路径；
- 当前 Season 路径；
- CID（弱化显示）；
- 打开 115 目录。

例如：

```text
115 存储
影视库 / 美剧 / 最后生还者 (2023) / Season 02
```

### Mobile 释放入口

移动端不在详情页底部单独放整行大号“释放”按钮。

`释放空间` 作为低频危险次级操作，收进 `115 存储` 卡片内部，并提示：

```text
清空当前 Season 目录内容，并暂停追更
```

点击后再弹轻量确认。

## 11.4 操作区

追剧详情与追剧卡片保持同一套核心操作，不让用户必须进入技术页面才能操作：

### 显式按钮

- `升级画质`；
- `暂停追更 / 恢复追更`；
- `停追`；
- `释放空间`。

### `···` 菜单

- `立即检查`；
- `打开 115 目录`；
- `重新绑定目录`；
- `移出资源黑名单`（仅存在黑名单记录时）。

操作原则：

- `升级画质` 为**当前 Season 的手动任务**；
- 暂停追更时同时暂停正在运行的画质升级任务；
- 停追时取消该 Season 的画质升级任务；
- 危险操作继续使用轻量确认。

## 11.5 最近活动

默认只展示结果型事件：

- 发现第 30 集；
- 已补齐第 30 集；
- 当前无更新；
- 补集失败，自动尝试下一候选；
- 目录整理完成；
- 已恢复，缺失剧集已补齐。

默认隐藏：

- 来源频道；
- 磁力；
- InfoHash；
- HTTP 详情；
- 搜索结果总数；
- 技术错误栈。

高级日志仅用于调试。

---

# 12. 设置页

整体结构：

```text
追剧设置
115 网盘
资源搜索频道
搜索源
代理设置
```

PC：左侧设置分类导航 + 右侧具体内容。  
Mobile：纵向配置入口卡片。

## 12.1 追剧设置

V1 全局设置只保留**默认目标画质**：

```text
默认目标画质
● 4K / 2160P
○ 1080P
```

**不提供“全局自动画质升级”开关。**

画质升级改为用户在追剧卡片/订阅详情中对某个 Season 主动点击 `升级画质`。

系统内置规则：

- 最低接受 1080P；
- `unknown` 画质可作为最低优先级候选；
- 明确 720P 及以下直接淘汰；
- 每小时检查；
- 每轮最多实际尝试 2 个有效候选；
- 释放后暂停追更；
- 电影保存成功后停止调度；
- btbtla 每 12 小时做一次兜底校准。

## 12.2 115 网盘

保留/重写已验证的 115 连接能力，并增加 8 类影视目录映射，一级目录为 `影视库`。

展示：

- 当前连接状态；
- 账号信息；
- 测试连接；
- 目录映射。

目录映射全部通过文件夹选择器完成。

## 12.3 资源搜索频道（Search Channels）

Search Channels 是 V1 的核心资源搜索配置。新服务自行实现，不依赖 CloudSaver 服务。

保留：

- 频道列表；
- 拖拽排序；
- 启用 / 禁用；
- 频道名称；
- Telegram `channelId`；
- 单频道检测；
- 检测全部；
- 新增频道保存前检测；
- 批量导入；
- 编辑 / 删除；
- 保存设置。

UI 原则：

- 紧凑列表；
- 明确提示“排序越靠前，自动找资源时越优先”；
- 默认导入时包含 `115` 的频道优先；
- 用户拖拽后尊重用户排序；
- 检测状态显示为 `正常 / 异常 / 未检测`；
- 异常只标记，不自动删除/禁用。

后端使用显式 `sort_order` 持久化排序。

## 12.4 搜索源

搜索源与 Telegram Search Channels 分离。

- **Search Channels**：核心资源来源，直接抓 Telegram 公开频道；
- **btbtla 等搜索源**：历史影视搜索、资源补充、最新集校准。

btbtla 资源调用策略：

```text
按需调用 + 每 12 小时兜底校准
```

只有 Telegram 无法完整解决当前待补集、无法可靠确认最新集，或达到 12 小时校准周期时才调用，避免每小时对所有订阅无条件请求。

## 12.5 代理设置

设置页保留 CloudSaver 原有代理配置方式：

```text
启用代理
HTTP Proxy Host
HTTP Proxy Port
测试连接
```

Clash 独立 Docker 部署；Telegram、btbtla、TMDB 等外部请求统一可按配置走 Clash。

代理故障不得阻塞主服务启动，也不得让订阅主流程整体失败。

## 12.5.1 TMDB 元数据增强（实验）

TMDB 仅用于可选元数据增强与 `tmdbId` 匹配测试。

用户已经申请 TMDB API Key / API Read Access Token。真实密钥不写入 PRD，只在部署环境保存：

```text
TMDB_ENABLED
TMDB_API_KEY
TMDB_READ_ACCESS_TOKEN
```

要求：

- 默认异步调用；
- 直连慢/失败时允许走 Clash；
- TMDB 失败不得阻塞订阅、建目录、搜索、补集；
- 可通过 `TMDB_ENABLED=false` 完全关闭；
- 若测试证明收益有限或影响稳定性，可以从正式流程中舍弃。

## 12.6 豆瓣个性化（V1 不保留）

V1 不提供豆瓣个性化设置，不保存豆瓣 Cookie，不实现：

- `为你推荐`；
- `想看同步`；
- 多 profile。

发现页仅保留无需用户配置的 11 类豆瓣热榜数据。

CloudSaver 0.9.0 中的 `watch_profiles / interests` 逻辑可作为后续版本“同步豆瓣想看”功能的参考，但不进入 V1。

## 12.7 TG Bot（V1 不保留）

CloudSaver 的 TG Bot 仅用于频道维护入口，资源搜索本体不依赖 Bot API。V1 不部署、不实现 Telegram Bot 服务。

---

# 13. 状态机与关键业务规则

## 13.1 订阅状态

电视剧订阅粒度为**Season**。用户控制状态与生命周期状态分开：

```text
subscriptionStatus = following / paused / stopped
lifecycleStatus    = active / completed
```

用户控制状态：

```text
追更中(following)
  ├─ 暂停 → 已暂停(paused)
  └─ 停追 → 停追(stopped) → 历史

已暂停
  └─ 恢复 → 追更中

停追
  └─ 重新追 → 追更中
```

完成判定：

> **只有存在可靠完结/总集数信号，并且 `missingEpisodes=[]`，才将该 Season 的 `lifecycleStatus` 标记为 `completed`、停止小时级调度并进入历史“已完成剧集”。**

可靠完结/总集数信号包括：

- btbtla 详情页明确的 `集数：N`，作为总集数的重要辅助信号；
- btbtla / Telegram 资源明确出现 `全 N 集 / 全集 / 完结`；
- 豆瓣等已有元数据明确给出 `totalEpisodes=N`；
- TMDB 总集数仅作为实验性增强信号，不能成为必要依赖。

`更新至 N 集`、连续多天无更新等都**不能单独**判定完结。若来源给出 `全 N 集` 或总集数 N，仍必须确认当前 Season 已覆盖 `E01...EN`、`missingEpisodes=[]` 后才能 completed。

建议保存：

```text
totalEpisodes
completionConfirmed
completionSource
```

完结确认不是永久锁死：如果后续任一可靠来源发现 `resolvedLatestEpisode > totalEpisodes`，原完结信号立即失效，`completionConfirmed=false`，该 Season 自动从 `completed` 恢复 `active/following` 并继续补新集。

如果 Season 目录扫描后已经完整，但来源尚未明确完结，则仍保持 `following + active`；如果创建订阅时已有完整 Season 目录且完结信号可靠，可直接进入 completed。

## 13.2 运行状态

```text
等待更新
检查中
补集中
异常
已释放
```

订阅状态与运行状态分离。

## 13.3 电影状态

电影保存成功后：

```text
已保存
→ 历史
→ 停止调度
```

## 13.3.1 订阅级画质升级

画质升级不是全局自动任务，而是用户对某个 Season 手动触发的临时任务。

执行条件：

```text
missingEpisodes = []
```

只要当前季仍有历史缺集或最新更新集未补齐，永远先补集，不执行升级。

升级流程：

```text
用户点击“升级画质”
→ 扫描当前 Season 已有画质
→ 找出低于目标画质的 Episode
→ 搜索更高画质候选
→ 每轮最多 2 个有效候选
→ 先转存高画质文件
→ 验证 season+episode 已正确落盘
→ 再删除对应旧低画质文件
```

禁止“先删旧文件再下载高画质”。

暂停追更时暂停升级任务；停追时取消升级任务。

## 13.4 释放与停追的关系

释放 ≠ 停追。

释放：

- 清空 115 内容；
- 保留文件夹；
- 保留订阅；
- 默认暂停追更。

停追：

- 用户结束自动追踪；
- 不再调度；
- 进入历史；
- 115 内容可继续保留，也可后续释放。

---

# 14. PC / Mobile 设计差异

## 14.1 PC

- 顶部一级导航；
- 高信息密度；
- 追剧使用横向信息卡；
- 详情可展示剧集状态网格；
- 设置采用左导航 + 右内容。

## 14.2 Mobile

- 底部 Tab；
- 发现页采用**同页纵向热门分区流**，不以顶部分类 Tab 作为主要浏览方式；
- 每个热门分区内采用 **3 列竖版海报网格**，每个分类首页展示 **3 行（共 9 个海报）**；
- 同一分类内按**年份倒序**排列，同一年份保持原接口顺序；
- 热门分区固定保持 CloudSaver 0.9.0 已确认的 11 个热榜分类与原始接口顺序；
- 海报右上角展示 CloudSaver 0.9.0 原生五档计算标签：`待评分 / 神作 / 推荐 / 可看 / 一般`；
- 当前追剧、历史、异常统一采用**紧凑横向卡片**；
- 卡片高度以海报和上下内边距为基准，所有摘要信息和操作都收在卡片右侧区域，不额外向下拆行；
- 不展示剧集状态网格；
- 设置采用入口卡片；
- 高风险操作继续使用确认。

## 14.3 共同原则

- 同功能、同数据；
- 不做简单等比缩放；
- 移动端优先显示结果；
- 避免长表单与大段技术信息。

---

# 15. 性能与异步原则

用户明确要求界面响应快、使用舒服，因此：

## 15.1 页面首屏

首屏优先使用：

- 本地数据库；
- 缓存；
- 最近一次已知状态。

不要因为打开页面就阻塞等待：

- Search Channels 实时资源搜索；
- 搜索源；
- 115 全目录扫描。

## 15.2 后台更新

长耗时操作：

- 资源查询；
- 115 扫描；
- 离线任务；
- 文件验证；
- 补集；

全部异步执行。

前端刷新最终状态即可。

## 15.3 不因打开页面触发重扫描

浏览追剧页或详情页不等于重新扫描 115。

115 状态主要由：

- 每小时调度；
- 订阅创建；
- 用户手工立即检查；
- 必要的后台任务；

进行更新。

---

# 16. 数据模型建议

## 16.1 Series / Subscription

电视剧采用 Series 父对象 + Season 订阅模型。

`Series` 建议至少包含：

```text
id
seriesTitle
aliases
seriesYear
mediaType
tmdbId?               // 可选异步增强
storageCategory
targetSeriesCid
targetSeriesPath
createdAt
updatedAt
```

`Subscription` 建议至少包含：

```text
id
seriesId
seasonNumber
seasonYear
subscriptionStatus
lifecycleStatus
runStatus
targetSeasonCid
targetSeasonPath
resolvedLatestEpisode
pendingLatestEpisode
totalEpisodes
completionConfirmed
completionSource
existingEpisodeKeys
missingEpisodeKeys
processingEpisodeKeys
targetQuality
qualityUpgradeStatus
preferredGroupKey
consecutiveFailRounds
lastBtbtlaCalibratedAt
lastCheckedAt
createdAt
updatedAt
```

内部 Episode 唯一键统一为：

```text
S01E01
S02E01
S00E01
```

## 16.2 Resource / Candidate

内部使用即可，不在普通 UI 暴露：

```text
source
resourceId
sourceChannelId
shareCode
receiveCode
seasonNumber
episodeKeys
missingCoverageCount
coversAllMissing
completePack
quality            // 2160p / 1080p / unknown
channelSortOrder
preferredGroupHit
status
createdAt
```

资源失败记录建议统一维护：

```text
sourceType          // pan115 / magnet
candidateKey        // shareCode / infoHash
failureCount
lastFailedAt
failureReason
isBlacklisted
```

## 16.3 CleanupCandidate

```text
subscriptionId
episode
keepFileId
removeFileId
keepQuality
removeQuality
reason
status
```

## 16.4 MediaMetadata

```text
id
sourceId
source
title
aliases
year
mediaType
region
genres
posterUrl
backdropUrl
rating
recommendation
latestEpisode
totalEpisodes
summary
```

---

# 17. V1 范围

## 17.1 V1 必做

- 发现页；
- 热门影视；
- 统一搜索；
- 影视详情；
- 一键追剧；
- 当前追剧；
- 历史（含已完成剧集 / 已完成电影 / 已停追）；
- 异常；
- 待清理；
- 115 `影视库` + 8 类 CID 映射；
- 自动目录创建与扫描；
- 一季一个订阅 + Series/Season 目录复用；
- TMDB 兼容命名（TMDB ID 为可选异步增强）；
- Telegram Search Channels 核心资源搜索；
- 搜索源兜底（btbtla magnet → 115 离线）；
- 每小时追更；
- btbtla 按需调用 + 每 12 小时校准；
- latestEpisode 异常跳增保护；
- 缺集补齐；
- 电影一次性保存；
- 释放；
- 暂停 / 恢复 / 停追 / 重新追；
- 默认目标画质；
- 订阅级手动画质升级；
- 自研 Search Channels 配置（排序 / 启停 / 检测 / 批量导入）；
- CloudSaver 原代理配置交互 + 独立 Clash；
- PC / Mobile 响应式差异设计。

## 17.2 V1 不做

- 多网盘；
- 用户手工挑选资源；
- 普通 UI 展示磁力 / InfoHash / 频道技术详情；
- 复杂资源管理中心；
- 单集释放；
- 每个订阅长期自动运行独立画质策略（仅允许用户主动触发当前 Season 的升级任务）；
- 自研推荐算法（右上角推荐标签直接复刻 CloudSaver 0.9.0 五档规则）；
- 豆瓣 Cookie / 为你推荐 / 想看同步；
- Telegram Bot；
- 其他网盘链接解析与转存；
- 复杂通知中心；
- 打开页面即时重扫全部 115；
- 复杂排序和筛选器。

---

# 18. 后续开发映射

## 18.1 总体技术路线（已确认）

新产品采用**独立自研单服务架构**，CloudSaver 0.9.0 与历史 subscriber 仅作为逆向参考：

```text
新前端：发现 / 追剧 / 设置
            ↓
独立后端服务
  ├── Media
  ├── Resource
  ├── 115 Adapter
  ├── Subscription
  ├── Scheduler
  └── Setting
            ↓
     115 / Telegram / btbtla
```

明确不采用：

- CloudSaver 服务 + subscriber 服务拼接；
- 反向代理注入订阅按钮；
- CloudSaver 运行时依赖；
- TG Bot；
- 多网盘模块；
- 豆瓣个性化 Cookie 模块。

## 18.2 从 CloudSaver 0.9.0 参考并重写的能力

仅保留对 V1 有直接价值的已验证逻辑：

- 豆瓣 11 类热门影视接口与元数据归一化；
- 评分展示与五档推荐标签计算；
- Telegram Search Channels 的 `t.me/s` 抓取、HTML 解析与频道检测；
- 115 分享链接识别；
- 代理配置交互；
- 115 `folders / save / share-info` 等已验证能力。

另外结合历史 subscriber 中的 115 Adapter 逻辑，自行实现目录创建、重命名、扫描、组织等新产品必需能力。

## 18.3 不迁移的 CloudSaver 能力

V1 明确不迁移：

- 夸克 / 123 / 天翼 / 百度 / 阿里 / UC / 迅雷等多网盘链路；
- Telegram Bot；
- Push Flows；
- Plugins；
- UserManagement / Admin 管理体系（除非后续另行确认）；
- Extension；
- 豆瓣 Cookie、为你推荐、想看 profiles；
- 与 V1 自动追剧无关的原项目配置。

## 18.4 已确认的 Discover 数据规则

- 发现页使用 CloudSaver 0.9.0 的 11 个热榜分类；
- 每分类首页 3×3，共 9 张海报；
- 分类内按年份倒序，同年保持原顺序；
- 年份直接从 `card_subtitle` 提取，不补精确上映日期；
- 左上角显示豆瓣评分；
- 右上角复刻 CloudSaver 0.9.0 五档标签：`待评分 / 神作 / 推荐 / 可看 / 一般`。

## 18.5 已确认的 Search Channels 规则

- Telegram 搜索使用公开 `t.me/s` 页面抓取，不使用 Bot API；
- 固定并发 6；
- 单频道超时 10 秒；
- 按 `sort_order` 优先级分批搜索；
- 找到足够候选提前停止；
- 搜索候选池可大于 2，但单轮最多**实际尝试 2 个有效候选**；
- 默认导入时包含 `115` 的频道优先；
- 用户拖拽后以用户排序为准；
- 支持单个检测、检测全部、新增保存前检测；
- 检测异常不自动删除/禁用；
- V1 只提取 115 分享链接。

---

## 18.6 已确认的 Season / 目录规则

- 电视剧一季一个订阅；
- 同一剧共享 Series 父目录；
- 父目录优先按 tmdbId，其次 title+year 匹配；
- 已有目录不自动重命名；
- 新建季目录统一 `Season XX`；
- 内部剧集唯一键为 `season + episode`；
- 多季分享必须保留父目录上下文。

## 18.7 已确认的候选与补集规则

- `missingEpisodes` 同时包含历史断集和最新更新集；
- latestEpisode 由订阅历史、Telegram/Search Channels、btbtla 等共同取最大值，只前进不后退；
- 单次跳增 >10 集进入二次确认；
- 候选优先补完整，再考虑画质；
- 画质档位：`4K/2160P > 1080P > unknown`，明确 720P 及以下淘汰；
- 每轮最多实际尝试 2 个有效候选；
- 第一个候选执行后重新扫描并重算缺集，再决定是否尝试第二个；
- 115 shareCode 连续失败 2 次进入永久黑名单。

## 18.8 已确认的画质升级与目录清洁规则

- 不做全局自动画质升级；
- 用户在追剧卡片/详情中主动对当前 Season 点击 `升级画质`；
- 缺集未补齐时不执行升级；
- 高画质文件验证成功后才能删除旧低画质文件；
- 115 最终影视目录只保留正片视频文件，非正片附件统一清理。

## 18.9 已确认的 TMDB 策略

- TMDB 为实验性、可选、异步增强；
- 用户已申请 API Key / Read Access Token；
- 真实密钥只放部署环境，不写文档明文；
- 直连慢时允许走 Clash；
- TMDB 失败不得阻塞主任务；
- 未拿到 tmdbId 时继续使用 `名称 (年份)` 目录；
- 已创建的无 tmdbId 目录后续不因异步匹配成功而自动重命名。

## 18.10 已确认的完成、失败与电影规则

- completed Season 自动进入历史“已完成剧集”；
- 用户控制状态 `following/paused/stopped` 与生命周期 `active/completed` 分离；
- btbtla `集数：N`、`全N集/全集/完结`、可靠 totalEpisodes 等共同提供完结确认信号；
- `更新至N集` 或长时间无更新不能单独判完结；
- 后续发现更高集数时撤销 completed 并恢复追更；
- 115 按 shareCode、btbtla magnet 按 infoHash 做失败记录，连续 2 次资源级失败永久黑名单；
- 网络/代理/服务超时不计入永久黑名单；
- 电影多视频候选先排除预告/花絮/Sample 等，再优先选择剩余文件中体积最大的主视频。

# 19. 最终产品体验总结

最终产品应当让用户的日常行为尽可能简单：

```text
打开“发现”
   ↓
找到想看的影视
   ↓
点击进入详情
   ↓
点击“追剧”
   ↓
看到“已加入追剧”
   ↓
结束
```

之后系统自动负责：

```text
Telegram Search Channels 找 115 资源
→ 必要时 btbtla 补充 / 每 12 小时校准
→ 扫描 115
→ 补最新集
→ 补中间缺集
→ 验证文件
→ 整理目录
→ 识别重复
→ 持续每小时追更
```

用户平时只需要关注：

```text
更新到多少
115 已有多少
有没有缺集
当前是否异常
是否需要释放空间
```

这就是 V1 的核心价值。

---

# 20. UI 最终调整与 Figma 差异说明（开发交付基准）

> 本章节用于解决“最终 PRD 已确认，但 Figma MCP Starter 月度调用额度已耗尽，无法把最后一轮修改写回原 Figma 文件”的问题。
>
> **开发时以本 PRD 为唯一产品与交互基准。** 当前 Figma 文件仅作为布局、视觉语言、间距、圆角、字体层级和 PC/Mobile 结构参考；若 Figma 与本章节或前文冲突，以本 PRD 为准。

## 20.1 当前 Figma 状态

- 当前 Figma 中已经存在完整的 PC / Mobile 发现、追剧、详情、设置等页面设计；
- 当前设置页仍停留在额度耗尽前的版本，存在旧入口和旧文案；
- 最后一轮最终需求**尚未真正写回 Figma 节点**，不是设计未确定，而是 Figma MCP Starter 调用额度达到上限；
- 当前可交付的最新 UI 参考图：
  - `115追剧_设置页_PC_最终版.png`
  - `115追剧_设置页_Mobile_最终版.png`
  - `115追剧_设置页_UI设计图_最终版.png`
- AI 开发实现时，先按本章把旧 Figma 设置页做逻辑纠偏，再沿用原 Figma 的整体视觉风格。

## 20.2 设置页最终信息架构

设置页最终只保留以下 5 个入口：

```text
追剧设置
115 网盘
资源搜索频道
搜索源
代理设置
```

必须删除旧 Figma 中以下入口 / 区块：

```text
CloudSaver 资源
豆瓣
项目配置
全局自动画质升级
```

说明：

- `CloudSaver` 只作为逆向参考来源，不是新产品运行时模块，因此 UI 中不再出现“CloudSaver 资源”命名；
- 豆瓣个性化（Cookie / 为你推荐 / 想看同步）V1 不保留；
- 原 CloudSaver 项目配置、插件、扩展动作等不属于 V1；
- 画质升级为 Season 订阅级手动任务，不再做全局自动升级开关。

## 20.3 PC 设置页最终布局

PC 继续沿用当前 Figma 的“顶部导航 + 左侧设置分类 + 右侧内容区”结构，不改整体视觉语言。

左侧导航：

```text
追剧设置
115 网盘
资源搜索频道
搜索源
代理设置
```

### 20.3.1 追剧设置

只保留：

```text
默认目标画质
● 4K / 2160P
○ 1080P
```

辅助说明：

> 画质升级需在追剧卡片或订阅详情中针对当前 Season 手动触发。

不出现“自动画质升级”全局开关。

### 20.3.2 115 网盘

顶部连接卡：

```text
连接状态                         ✓ 已连接
账号信息（可弱化）               [测试连接]
```

影视目录映射必须补齐为 8 类：

```text
国产剧      影视库 / 国产剧               [重新选择]
美剧        影视库 / 美剧                 [重新选择]
日韩剧      影视库 / 日韩剧               [重新选择]
电视剧      影视库 / 电视剧               [重新选择]
综艺        影视库 / 综艺                 [重新选择]
动漫        影视库 / 动漫                 [重新选择]
纪录片      影视库 / 纪录片               [重新选择]
电影        影视库 / 电影                 [重新选择]
```

目录状态展示：

```text
✓ 正常
未配置
目录不可访问
```

CID 不作为主展示字段，只在必要的高级信息中弱化显示；实际后端执行始终使用 CID。

### 20.3.3 资源搜索频道

这是 V1 的核心设置页面之一，PC 建议使用紧凑可拖拽列表。

顶部说明：

> 排序越靠前，自动找资源时优先级越高。

操作区：

```text
[+ 添加频道] [批量导入] [检测全部]                       [保存设置]
```

列表每行至少展示：

```text
拖拽手柄
频道名称
Telegram channelId
状态：正常 / 异常 / 未检测
启用开关
[检测] [编辑] [删除]
```

示例：

```text
☰ Lsp115              Lsp115                  正常    开启
☰ vip115hot           vip115hot               正常    开启
☰ Channel_Shares_115  Channel_Shares_115      正常    开启
```

规则：

- 初次导入时，name / channelId 中包含 `115` 的频道默认排前；
- 用户拖拽保存后，以用户 `sort_order` 为准；
- 检测异常只标记，不自动删除、不自动禁用；
- 新增频道保存前先检测；
- 支持单频道检测和检测全部。

### 20.3.4 搜索源

搜索源页与 Telegram Search Channels 分离。

至少展示：

```text
btbtla
状态：正常 / 异常
用途：历史影视搜索 / 资源补充 / 每 12 小时最新集校准
调用方式：按需 + 12 小时兜底
[测试连接]
```

TMDB 作为实验性增强项放在同页次级区域：

```text
TMDB 元数据增强                实验
状态：已配置 / 未配置 / 连接异常
用途：可选 tmdbId 匹配与命名增强
失败不会影响追剧主流程
[测试连接]
```

不得把 TMDB 设计成必须配置才能使用系统。

### 20.3.5 代理设置

交互沿用 CloudSaver 原代理配置方式，但视觉上归入新产品设置页：

```text
启用代理              [开关]
代理地址              clash
代理端口              7890
代理协议              HTTP / SOCKS5（按后端最终实现能力）
                      [测试连接]
```

说明文案：

> Telegram、btbtla、TMDB 等外部访问可按配置使用代理；代理异常不得阻塞主服务启动。

## 20.4 Mobile 设置页最终布局

Mobile 不使用 PC 左侧导航，继续采用纵向入口卡片 + 二级设置页。

首页入口卡片最终为：

```text
追剧设置
默认目标画质

115 网盘
连接状态、8 类影视目录

资源搜索频道
频道排序、启停、检测

搜索源
btbtla、TMDB 实验增强

代理设置
Clash / HTTP 代理
```

删除旧 Mobile Figma 中：

```text
CloudSaver 资源
豆瓣
项目配置
“默认画质、自动画质升级”中的自动升级描述
```

Mobile 的 115 目录页必须完整展示 8 类目录，不能只显示 4 类或 6 类。

## 20.5 追剧卡片最终 UI 基准

追剧页卡片必须直接展示核心业务状态，不能要求用户进入技术详情页才能判断当前情况。

PC / Mobile 都至少展示：

```text
当前季：Season XX
最新集：E XX
115 已有：X / Y
缺失集：E03、E07… / 无缺失
目标画质：4K / 2160P 或 1080P
115 存储路径
运行状态
最近检查时间
```

直接操作：

```text
升级画质
暂停 / 恢复
停追
释放空间
```

其中：

- `升级画质`：只针对当前 Season，用户手动触发；
- `暂停`：暂停追更并暂停当前 Season 的画质升级任务；
- `停追`：停止该 Season 后续调度并取消画质升级任务；
- `释放空间`：清空当前 Season 内容、保留目录与订阅，并默认暂停追更。

Mobile 操作必须收进同一卡片右侧 / 右下紧凑区域，不能把单独按钮拆成占满整行的大块。

## 20.6 历史页最终 UI 调整

历史筛选最终为：

```text
全部 | 已完成剧集 | 已完成电影 | 已停追
```

completed Season 自动进入“已完成剧集”。

若后续发现更高集数：

```text
completed
→ 自动恢复 following
→ 从历史重新进入当前追剧
```

无需用户手工重新追。

## 20.7 电影候选与成功判定 UI / 逻辑说明

电影分享里可能存在多个视频文件，系统选择主正片时：

1. 先排除标题含 `预告 / Trailer / 花絮 / Behind / Sample / 片段` 等明显非正片；
2. 在剩余有效视频文件中优先选择体积最大的主视频；
3. 目标电影目录中出现有效正片文件后，才标记 `已保存`；
4. 最终目录仅保留正片视频，其他附件清理。

有效视频扩展名至少：

```text
mkv / mp4 / ts / mov / m2ts
```

## 20.8 资源失败与永久黑名单最终规则

资源失败必须区分“资源本身失效”和“基础设施异常”。

### 115 分享

```text
candidateKey = shareCode
```

### btbtla magnet

```text
candidateKey = infoHash
```

连续 2 次**资源级失败**后进入永久黑名单。

计入失败次数：

```text
分享已失效
shareCode 无效
提取码错误
资源不存在
115 明确返回分享不可用
magnet / infoHash 明确无效
```

不计入失败次数：

```text
HTTP timeout
DNS 异常
代理异常
115 服务临时异常
btbtla 暂时不可访问
任务取消 / 服务重启
```

开发时不得因为网络故障误把正常资源永久拉黑。

## 20.9 单轮候选上限最终定义

“一轮最多 2 个有效候选”是**跨所有来源合计最多 2 个资源方案**，不是每个来源各 2 个。

例如：

```text
候选 1：Telegram 115 分享
→ 失败

候选 2：btbtla magnet
→ 成功

结束本轮
```

不能执行成 Telegram 2 个 + btbtla 2 个 = 4 个。

## 20.10 候选排序最终实现顺序

最终 comparator：

```text
1. coversAllMissing = true 优先
2. missingCoverageCount 越大越优先
3. 整季包 / 全集包优先
4. 画质：4K / 2160P > 1080P > unknown
5. search_channel.sort_order
6. preferredGroupKey 命中（最后一级弱偏好）
```

核心原则仍然是：

> **补完整优先于画质。**

## 20.11 latestEpisode 与 Search Channels 提前停止说明

Search Channels 找到足够候选后允许提前停止，以减少 Telegram 请求量。

因此：

- 本轮 Telegram `latestEpisode` 只代表**本轮已查询频道中观察到的最大值**；
- 不保证每小时都扫描全部频道得到绝对全局最大值；
- btbtla 按需查询 + 每 12 小时校准用于补偿提前停止带来的漏发现风险；
- 若任意已查询来源发现更高集数，仍应推进 `resolvedLatestEpisode`。

## 20.12 Season 完结确认最终规则

Season 只有同时满足：

```text
存在可靠完结 / 总集数信号
+
115 当前 Season 已覆盖全部目标集数
+
missingEpisodes = []
```

才进入 `completed`。

可靠信号包括：

```text
btbtla 详情页：集数 N（重要辅助信号）
btbtla / Telegram：全 N 集 / 全集 / 完结
可靠元数据 totalEpisodes
TMDB 总集数（仅增强，不作为必要依赖）
```

注意：

- `更新至 N 集` 只能说明当前发现进度，不代表完结；
- 长时间无更新不能单独判完结；
- `btbtla 集数：N` 是重要辅助信号，但不能在 115 尚缺集时直接 completed；
- 如果后续 Telegram / btbtla 等任一来源发现 `N+1` 或更高集数：

```text
completionConfirmed = false
completed → active / following
resolvedLatestEpisode 向前推进
重新计算 missingEpisodes
继续追更
```

推荐记录：

```text
totalEpisodes
completionConfirmed
completionSource
```

普通 UI 不需要向用户暴露 `completionSource`。

## 20.13 Series 年份、Season 年份与目录命名

开发模型必须区分：

```text
Series.seriesYear   // 整部剧首播年份
Subscription.seasonYear // 当前 Season 播出年份
```

例如：

```text
最后生还者
seriesYear = 2023
Season 02
seasonYear = 2025
```

Series 父目录仍使用：

```text
最后生还者 (2023)
```

不能因为订阅第二季而创建：

```text
最后生还者 (2025)
```

TMDB ID 规则：

- 创建目录时本地已有 `tmdbId` → 可使用 `[tmdbid-ID]`；
- 创建时还没有 → 直接使用 `名称 (年份)`；
- 后续 TMDB 异步匹配成功，只写数据库，不自动重命名已经存在的 Series 目录。

## 20.14 AI 开发实施优先级

AI 开发时建议严格按以下顺序推进，避免 UI 与后台并行发散：

```text
1. 数据模型 / 状态机
2. 115 Adapter 与目录模型
3. Search Channels
4. btbtla 搜索源与 magnet fallback
5. Scheduler / missingEpisodes / latestEpisode
6. 候选排序、失败黑名单、验证与清理
7. 发现页 / 详情页
8. 追剧 / 历史 / 异常 / 待清理
9. 设置页（严格按本章最终 IA）
10. TMDB 实验增强
11. PC / Mobile 视觉还原
```

## 20.15 开发验收时的 Figma / PRD 优先级

验收优先级固定：

```text
业务规则：PRD > 旧 Figma
信息架构：PRD > 旧 Figma
字段/状态：PRD > 旧 Figma
视觉风格：Figma > 文字描述
最终设置页内容：本章 > 旧 Figma 设置页
旧figma链接:https://www.figma.com/design/7rVN6gcXLdSaNL2x5PZUqL/115%E5%BD%B1%E8%A7%86%E8%AE%A2%E9%98%85%E4%B8%8E%E8%87%AA%E5%8A%A8%E8%BF%BD%E5%89%A7%E6%9C%8D%E5%8A%A1_V1_UI?node-id=3-6&t=L5RhVlyPOK7ZhG4Z-0
ui可以直接看旧figma,只是最新的功能需要调整一下ui而已
```

如果 AI 开发过程中发现旧 Figma 与本 PRD 冲突：

> **不得自行折中，直接按本 PRD 实现。**

---

# 21. 技术架构、部署与开发协作（最终确认）

## 21.1 技术栈

V1 采用前后端分离、单后端服务架构，技术栈固定为：

```text
前端：React + TypeScript + Vite
后端：TypeScript + Fastify
数据库：PostgreSQL
后台任务：pg-boss（复用 PostgreSQL，不额外引入 Redis）
反向代理 / 静态资源：Caddy 或 Nginx，开发时根据现有环境二选一
部署编排：Docker Compose
```

选择原则：

- PC 与 Mobile 共用响应式 Web 前端，优先保证打开速度、操作流畅和界面简单；
- 支持 PWA，可添加到手机桌面并提供必要的静态资源缓存；
- V1 不开发原生 App、推送通知或复杂离线能力；
- 前后端统一使用 TypeScript，降低独立维护和 AI 协作开发成本；
- 后台任务必须持久化，服务或 Docker 重启后能够恢复未完成任务；
- 不为了形式拆分微服务，V1 保持单后端服务和清晰的内部模块边界。

## 21.2 用户与访问模式

- V1 为单用户产品，仅供所有者本人使用；
- 不实现注册、邀请、租户、多用户角色或后台用户管理；
- 产品保留简单的账号密码登录保护；
- 115 凭据、TMDB 凭据及其他敏感配置只能保存在服务端，并以加密或 Secret / 环境变量方式管理；
- V1 完成后先部署在家庭局域网内，不把公网访问作为产品开发和验收范围；
- 公网访问由用户在部署后自行解决，Cloudflare Tunnel、Tailscale、FRP 等均不作为 V1 业务模块或强依赖。

## 21.3 家庭服务器部署

目标运行环境为：

```text
macOS 主机
└── Docker / Docker Compose
    ├── Web / Caddy（或 Nginx）
    ├── Fastify 后端
    ├── PostgreSQL + pg-boss
    └── Clash（沿用或接入家庭服务器现有配置）
```

部署要求：

- 所有运行组件必须支持 Docker Compose 一次启动；
- Web、API 和数据库数据使用明确的持久化目录或 Docker Volume；
- 默认只向局域网开放 Web 访问端口，PostgreSQL 不直接暴露给局域网或公网；
- 健康检查至少覆盖 Web、后端、数据库及后台任务执行能力；
- Telegram、btbtla、TMDB 等外部请求按设置决定是否经过 Clash；
- 代理异常不得阻塞主服务启动，115 本地扫描及前台已缓存内容仍应可用；
- 配置文件提供示例模板，真实密码、Cookie、Token、API Key 不得提交到 Git。

## 21.4 数据安全与备份

- PostgreSQL 每日自动备份，默认保留最近 7 天；
- 备份目录必须持久化到 Mac 主机，并与数据库容器生命周期分离；
- 升级数据库结构前必须先执行备份；
- 日志不得输出 115 Cookie、访问令牌、TMDB Token、账号密码或完整敏感请求头；
- V1 至少提供数据库恢复说明，不要求在普通 UI 中提供备份管理页面。

## 21.5 参考源码使用边界

最高优先级原则：

> **旧版项目、CloudSaver 0.9.0、历史 subscriber、抓取产物和测试脚本全部只作为技术参考。产品范围、业务规则、数据模型、状态机、目录结构、UI、调度策略和验收标准必须以本 PRD 为准。**

参考代码只能用于确认接口调用方式、字段兼容、已验证的边界条件和可复用算法思路，不能据此反向修改已经确认的产品方案。若旧代码与 PRD 冲突，直接舍弃旧行为并按 PRD 重新实现；若旧代码缺少 PRD 要求的能力，也必须按 PRD 补齐，不能以“旧版未实现”为理由缩减范围。

正式开发开始前，必须先由用户确认以下本机路径：

```text
CloudSaver 0.9.0 实际部署 / 源码路径
用户此前开发版本的项目路径
历史 subscriber（如单独存在）的路径
现有 Docker / Clash 配置路径
```

读取参考源码时必须遵循：

- 只定向检查本 PRD 明确依赖的豆瓣热门、Telegram Search Channels、115 Adapter、代理配置等代码；
- 115 登录、目录、转存、离线下载、扫描、移动和重命名等接口细节，可结合 CloudSaver 与用户旧版实现反推；
- 不通读、不迁移与 V1 无关的多网盘、TG Bot、插件、扩展、推送、用户管理等模块；
- 参考源码只用于确认已验证行为，新产品仍独立实现且不产生 CloudSaver 运行时依赖；
- 未确认上述路径前，不得猜测旧版接口或凭据格式并写入正式实现。

## 21.6 UI 开发基准

完整 UI 的开发依据固定为：

```text
旧 Figma 文件：全量页面布局与视觉语言参考
本地最终版 PNG：设置页最后一轮视觉参考
本 PRD 第 20 章：最终功能、字段、状态与信息架构修正
```

当前 Figma Starter MCP 调用额度可能限制自动读取。开发前应再次验证访问能力；若仍无法读取，则由用户提供关键页面导出图。该限制不得导致开发者自行重设计业务页面。

## 21.7 多线程 / 多 Agent 并行开发要求

正式开发时允许并建议使用多个开发线程或 AI Agent 按模块并行工作，以提高开发效率，但必须先完成共享契约，不能让各线程独立猜测数据结构和接口。

### 第一阶段：主线程先固化公共基础

并行开发前必须由主线程完成并提交：

```text
项目目录结构
TypeScript / lint / test 规范
环境变量模板
数据库 Schema 与迁移基线
核心枚举和状态机
内部领域模型
API 路由、请求和响应契约
统一错误模型
Docker Compose 基线
```

公共契约未稳定前，不同时开发多个相互依赖的业务模块。

### 第二阶段：按模块并行

建议拆分为以下开发线程：

```text
线程 A：115 Adapter、目录扫描、文件操作与目录模型
线程 B：Telegram Search Channels、btbtla 与候选归一化
线程 C：Subscription、Scheduler、missingEpisodes、状态机与任务恢复
线程 D：React 前端、响应式 PC / Mobile 页面与设置交互
```

在核心链路稳定后，再独立并行：

```text
候选排序、资源黑名单、验证与清理
TMDB 实验性增强
端到端测试、Docker 部署与备份恢复验证
```

### 第三阶段：主线程集成验收

- 每个线程只负责约定目录和模块，避免同时修改同一核心文件；
- 共享类型、Schema、API 契约和迁移只能由主线程统一修改；
- 子线程发现契约缺失或冲突时必须上报主线程，不得自行创建另一套字段或状态；
- 每个模块合并前必须通过自身测试，并提供输入、输出、失败场景和未完成项说明；
- 合并顺序遵循本 PRD 第 20.14 节的开发实施优先级；
- 最终端到端流程、数据库迁移、Docker 部署和 UI 一致性由主线程统一验收。

多线程开发的目标是并行完成边界清晰的模块，而不是同时修改同一条核心链路。若模块存在强依赖，应先串行确定契约，再并行实现。

---

# 22. 旧版项目定向逆向结论（开发输入）

> 本章来自 `work/work` 中旧版项目与 CloudSaver 0.9.0 定向文件的检查结果。只记录 V1 需要的已验证接口、行为和迁移边界。**本章是开发参考资料，不是对前文需求的替代或修订；任何冲突都以前文已确认 PRD 为准。旧代码中存在但 PRD 未要求的能力不会自动进入 V1，旧代码中缺失但 PRD 已要求的能力仍必须完整实现。**

## 22.1 已检查的最小文件范围

```text
work/work/cloudsaver-subscribe-service/src/adapters/pan115Adapter.js
work/work/cloudsaver-subscribe-service/src/adapters/cloudSaverAdapter.js
work/work/cloudsaver-subscribe-service/src/adapters/cloudSaverCookieProvider.js
work/work/cloudsaver-subscribe-service/src/adapters/authorizedSources/btbtlaAdapter.js
work/work/cloudsaver-subscribe-service/src/services/authorizedSourceService.js
work/work/cloudsaver-subscribe-service/src/services/subscriptionService.js（仅核心调度、候选、验证和目录片段）
work/work/cloudsaver-subscribe-service/src/scheduler.js
work/work/cloudsaver-subscribe-service/src/media/naming.js
work/work/cloudsaver-subscribe-service/src/media/matcher.js
work/work/cloudsaver-subscribe-service/src/config.js
work/work/cloudsaver-subscribe-service/docker-compose.yml
work/work/cloudsaver-douban-api-a.js
work/work/cloudsaver-douban-api-b.js
work/work/cloudsaver-userSetting.js
work/work/cloudsaver-login-flow.js
```

明确未通读：

- `node_modules`；
- 旧版注入 UI、样式和与 V1 无关的前端资源管理代码；
- CloudSaver 约 1 MB 的完整前端主包；
- 多网盘、TG Bot、插件、推送和用户管理模块。

## 22.2 115 已验证接口与请求约定

旧版 `Pan115Adapter` 已经验证以下直接调用方式，新版可按接口语义重写，但必须补充类型、超时、错误分类、重试和测试：

### 通用请求

默认 API 基址：

```text
https://webapi.115.com
```

请求头至少包含：

```text
Cookie: <115 Cookie>
Referer: https://115.com/
Origin: https://115.com
User-Agent: 浏览器 UA
```

以下情况视为失败：

```text
HTTP 非成功
响应无法解析为 JSON
payload.state === false
payload.errno 存在
payload.error 存在
```

实现时不能仅按 HTTP 状态判断成功；必须同时识别 115 业务错误字段。

### 目录与文件操作

```text
GET  /files
用途：列目录
参数：aid=1、cid、o=user_ptime、asc=0、offset、show_dir=1、limit、fc_mix=0

POST /files/add
Content-Type: application/x-www-form-urlencoded
参数：pid、cname
用途：创建目录

POST /files/batch_rename
Content-Type: application/x-www-form-urlencoded
参数：files_new_name[fileId]=targetName
用途：重命名

POST /files/move
Content-Type: application/x-www-form-urlencoded
参数：pid、fid[index]
用途：批量移动

POST /rb/delete
Content-Type: application/x-www-form-urlencoded
参数：fid[index]
用途：批量删除到回收站
```

115 文件返回字段存在多个版本，归一化时至少兼容：

```text
名称：n / name / file_name / fileName / cname
目录 ID：cid / id
文件 ID：fid / file_id / fileId / id
大小：s / size / file_size
PickCode：pc / pick_code / pickCode
目录判定：is_dir / isFolder / fc / ico / cid+fid 组合
```

`listFolder` 必须实现分页，不能把旧版默认单次 `limit=1150` 当成永远完整的目录结果。

### 115 磁力离线

旧版已验证流程：

```text
GET https://115.com/?ct=offline&ac=space
→ 获取 uid / sign / time

POST https://115.com/web/lixian/?ct=lixian&ac=add_task_url
Content-Type: application/x-www-form-urlencoded
参数：url、wp_path_id、uid、sign、time
→ 返回 info_hash 作为任务标识（字段可能位于 data.info_hash 或顶层）
```

若接口未直接返回 `uid`，旧版会从 Cookie 的 `UID` 字段提取。新版仍需在真实环境重新验证 Cookie 格式和错误码，不能把“提交成功”直接视为文件已经生成。

## 22.3 115 分享解析与转存的现状和缺口

旧版订阅服务没有独立完成 115 分享解析和转存，而是通过 CloudSaver 调用：

```text
GET /api/cloud115/share-info
参数：shareCode、receiveCode（可选）、pdirFid（展开子目录时可选）

POST /api/cloud115/save
Body：shareCode、receiveCode、fileId、folderId、fids
```

115 分享链接识别规则已验证为：

```text
(115|anxia|115cdn).com/s/{shareCode}?password={receiveCode}
```

旧版最多递归展开分享目录 4 层，并使用 `pdirFid` 逐层读取；它会维护 `visited folderId` 防止循环。

新版要求：

- 新产品不能调用 CloudSaver 上述接口，必须在独立 115 Adapter 中重写对应能力；
- 开发前需要继续从家庭服务器上的 CloudSaver 后端或旧容器反推 `share-info / save` 的真实 115 上游接口、请求字段和业务错误码；
- 递归展开必须保存 `parentPath` 和从父目录推导出的 Season，不能像旧版一样只返回拍平后的文件数组；
- 最大深度必须可配置，并同时设置最大文件数 / 目录数，避免异常分享导致无界遍历；
- 转存后必须重扫目标 Season 目录验证文件存在和剧集覆盖，API 返回成功不能作为任务完成条件。

这部分是正式开发前仍需现场反推的最高优先级接口缺口。

## 22.4 115 Cookie 配置与失效恢复（最终确认）

旧版曾支持两种 Cookie 来源：

```text
direct：环境变量直接提供 PAN115_COOKIE
cloudsaver-sqlite：只读 CloudSaver database.sqlite
```

CloudSaver SQLite 的旧读取方式为：

```sql
SELECT cloud115Cookie
FROM user_settings
WHERE cloud115Cookie IS NOT NULL AND cloud115Cookie != ''
ORDER BY updatedAt DESC
LIMIT 1;
```

旧版通过系统 `sqlite3` 命令只读查询，并缓存 Cookie 300 秒。

上述方式仅用于理解旧版行为，新版不迁移、不读取、不挂载 CloudSaver SQLite，也不保留旧数据库。

新版最终流程：

```text
用户自行获取 115 Cookie
→ 在“设置 / 115 网盘”页面粘贴
→ 点击保存并测试连接
→ 后端加密保存到新产品数据库
→ 后续 115 Adapter 从新产品配置读取
```

最终规则：

- V1 不实现 115 扫码登录、账号密码登录或自动抓取 Cookie；
- 设置页提供 Cookie 输入框、保存、测试连接和重新填写入口；
- 已保存后前端只显示脱敏值或“已配置”，API 不返回完整 Cookie；
- 保存新 Cookie 前先调用只读接口测试；测试成功才替换当前有效配置；
- 测试失败时不得覆盖原有可用 Cookie，并向用户显示简洁错误；
- 运行中检测到明确的登录失效 / Cookie 失效时，将 115 连接状态标记为“已失效”，暂停需要 115 写操作的任务；
- Cookie 失效不计为候选资源失败，不触发永久黑名单；
- 用户重新填写并测试成功后，恢复暂停的 115 任务调度；
- 日志、活动记录和错误响应必须遮蔽 Cookie；
- CloudSaver 的 SQLite 路径和 `user_settings.cloud115Cookie` 查询仅作为旧版逆向记录，不能进入新版运行时或部署配置。

## 22.5 豆瓣热门接口与 11 类参数

CloudSaver 前端已确认使用：

```text
GET /api/douban/hot
```

查询参数由 `type / category / api / start / count` 组成。11 类最终映射和顺序为：

| key | 标题 | type | category | api |
|---|---|---|---|---|
| hot-movie | 热门电影 | 全部 | 热门 | movie |
| latest-movie | 最新电影 | 全部 | 最新 | movie |
| classic-movie | 冷门佳片 | 全部 | 冷门佳片 | movie |
| hot-tv | 热门电视剧 | tv | tv | tv |
| tv-domestic | 热门国产剧 | tv_domestic | tv | tv |
| tv-american | 热门欧美剧 | tv_american | tv | tv |
| tv-korean | 热门韩剧 | tv_korean | tv | tv |
| tv-japanese | 热门日剧 | tv_japanese | tv | tv |
| tv-animation | 热门动画 | tv_animation | tv | tv |
| hot-show | 热门综艺 | show | show | tv |
| tv-documentary | 热门纪录片 | tv_documentary | tv | tv |

新版自行实现该后端数据链路；`recommendations / watch-list / douban credential / watch profiles` 等个性化接口不迁移。

## 22.6 Search Channels 已确认内容与待反推内容

旧版前端确认了以下设置接口：

```text
GET  /api/setting/get
POST /api/setting/save
POST /api/setting/update-channels
POST /api/setting/check-search-channel
GET  /api/setting/check-proxy
```

频道最小字段为：

```text
name
channelId
```

旧订阅服务通过 CloudSaver 调用：

```text
GET /api/search/channels?keyword=...
GET /api/search?keyword=...&channelId=...&lastMessageId=...
```

并识别分页字段的兼容形式：

```text
hasMore
nextMessageId / lastMessageId / nextLastMessageId / last_message_id
```

但当前 `work/work` 里能直接读取到的主要是 CloudSaver 前端构建产物和旧客户端 Adapter，没有完整的服务端 `t.me/s` 抓取实现。因此：

- PRD 已确认的公开 `t.me/s` 抓取、固定并发 6、单频道 10 秒超时和提前停止规则保持不变；
- 正式开发前只需在家庭服务器 CloudSaver 后端中继续定位 Search Channels 服务端实现；
- 重点只反推请求 URL、代理使用方式、HTML 选择器、分页边界、消息字段归一化及频道检测判定；
- 不再分析 CloudSaver 的资源管理 UI、Bot、Push Flow 或其他网盘解析；
- 旧订阅服务逐频道串行搜索的实现不得照搬，新版按 PRD 使用有界并发和全来源合计候选上限。

## 22.7 btbtla 已验证解析规则

旧版 `BtbtlaAdapter` 已确认以下流程：

```text
GET /search/{encodeURIComponent(title)}
→ 收集 /detail/{id}.html
→ 详情页收集 /tdown/... 下载入口
→ 下载页提取 magnet: 链接
→ 从 btih 提取并小写化 40 位 infoHash
```

连接检测通过首页存在 `/detail/` 链接或搜索表单判断页面结构兼容。外部请求使用浏览器式 User-Agent、默认 15 秒超时，并可通过 `undici ProxyAgent` 使用 HTTP 代理。

旧版已实现的可参考解析：

```text
SxxExx-Eyy
第 x-y 集
全 N 集
SxxExx
第 N 集
2160p / 4K
1080p
上映：YYYY
```

旧版只保留 2160p 和 1080p，并按“标题归一化后完全相等 + 年份相等”匹配。新版不得原样照搬其严格匹配，必须结合别名、Series/Season、标题中的季标识及弱匹配保护。

btbtla 候选仍需遵循前文最终规则：

- 明确 720P 及以下淘汰，unknown 可作为最后兜底；
- `infoHash` 是 magnet 候选唯一键；
- 资源级明确失败才累计黑名单次数；
- 网络、DNS、代理和站点超时不累计资源失败；
- btbtla 的 `集数：N / 全N集 / 全集 / 完结` 参与完结确认，但必须同时验证 115 覆盖和 `missingEpisodes=[]`。

## 22.8 已验证的提交后验证模式

旧版中值得保留的核心模式是：

```text
提交 115 分享转存或 magnet 离线
→ 等待
→ 重新扫描目标目录
→ 重新解析 Season + Episode
→ 重算 missingEpisodes
→ 成功后再整理 / 重命名
→ 仍缺集时才尝试下一个候选
```

旧版 115 分享转存后最多轮询 18 次、每次间隔 5 秒；btbtla 离线候选默认等待 5 分钟后进入验证。上述数字仅作为历史验证参数，不直接固定为新版产品规则。新版应将验证设计为 pg-boss 的持久延时任务，禁止在工作进程中长时间 `sleep` 或依赖内存定时器。

同一订阅必须有数据库级互斥 / 幂等键；旧版仅使用进程内 `runningSubscriptions Set`，无法覆盖多实例和服务重启场景。

## 22.9 旧版调度与新版替换要求

旧版调度方式为：

```text
setInterval 每小时扫描订阅
内存 running 标志防止整轮重叠
maxConcurrentRuns 控制并发
每分钟扫描 waiting_verification
```

新版统一替换为 PostgreSQL + pg-boss：

- 周期任务、立即检查、候选验证、画质升级、清理和 TMDB 增强均使用持久任务；
- 每项任务使用确定性的幂等键；
- 同一 Subscription / Season 同一类型任务不能并行执行；
- 服务重启后恢复未完成或到期任务；
- `paused / stopped / completed` 在任务领取和执行前都必须再次检查；
- 任务取消不能计为资源失败；
- 运行日志写入结构化 Run / Activity 数据，不只写文本日志。

## 22.10 文件解析和命名可复用边界

旧版已实现以下基础能力，可重写为独立纯函数并建立单元测试：

```text
Unicode NFKC 归一化
文件扩展名识别
4K / 2160P / UHD、1080P、720P 识别
SxxExx、SxxExx-Eyy、Season x + Episode、中文第x季第y集解析
文件名非法字符清理
TMDB 友好剧集文件名生成
```

旧版视频扩展名集合比最终 PRD 更宽，包含：

```text
mkv / mp4 / avi / mov / wmv / flv / ts / m2ts / webm
```

新版用于“有效正片”的扩展名以 PRD 最终范围为基础，额外格式是否支持应作为实现常量统一管理，不能在多个模块重复写正则。

## 22.11 明确禁止照搬的旧版行为

以下旧版实现与最终 PRD 冲突，只能作为反例或迁移线索：

- 旧版“一个剧一个目录、文件直接放剧目录、不建 `Season XX`”的扁平目录模型；
- 递归后丢失 `parentPath`，只按 episode 数字比较的多季处理；
- `defaultSeason=1` 覆盖所有剧集；
- 只配置国产剧 / 日韩剧两个父 CID；
- 把 `active / searching / transferring / failed / completed` 混在单一 status 字段；
- 仅依靠进程内 Set 和 `setInterval` 防重复；
- CloudSaver 账号、Token、SQLite 和运行时 API 依赖；
- 登录页“记住密码”时把账号密码保存到浏览器 localStorage；
- 候选排序优先比较 `latestEpisode`，而不是最终规则中的 `coversAllMissing / missingCoverageCount`；
- 把来源频道与画质作为候选分组主键，导致补集完整性可能被拆散；
- 未实现资源级失败与基础设施失败的严格分类；
- 未实现 8 类目录映射、Series 父目录复用和 Season 级订阅。

## 22.12 开发前剩余的定向反推清单

回到家庭服务器正式开发前，只需继续确认以下内容：

1. CloudSaver 后端中 115 `share-info / save` 对应的真实 115 上游接口、字段和错误码；
2. Search Channels 服务端 `t.me/s` 抓取、代理、HTML 解析、分页和检测实现；
3. 旧 Mac 上 CloudSaver Docker Compose 与 Clash 配置的实际路径；
4. 使用用户手动提供的有效 Cookie 进行只读接口验证时的最小测试样例。

除以上四项外，不再扩大 CloudSaver 逆向范围。涉及真实 115 写操作的测试必须使用专用测试目录，并在执行前由用户确认目录 CID；不得对现有影视库直接进行转存、移动、重命名或删除测试。
